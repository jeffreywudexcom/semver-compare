# Semantic Version Performance Test
Compares the performance of checking if a string is a semantic version using
- Initializing the CompareVersion instance
- Use regex

## Running
```
./gradlew build
./gradlew run
```
